<!DOCTYPE html>
<html lang="en">
<%- include ('../partials/head.ejs', {page: 'Artwork' }) %>
  
This is my nav bar now
<%- include ('../partials/nav.ejs') %>

    <section class="hero is-medium has-background-light">
    <div class="hero-body">
        <h1 class="title">artwork Show Page</h1>
    </div>
    </section>

    <div class="container is-max-widescreen p-5">
    <article class="media">
        <figure class="media-left">
        <p class="image is-328x328">
            <img src="<%= artworks.Image %>" alt="artwork img" />
        </p>
        </figure>
        <div class="media-content">
        <div class="content">
            <h3>Title: <%= artworks.Title %></h3>
            <small>Artist: $<%= artworks.Artist %></small>
            <small>Mediums: $<%= artworks.Mediums %></small>
            <small>Date: $<%= artworks.Date %></small>
            <div class="field mt-5 is-grouped">
            <div class="control">
                <a class="button is-info" href="/artwork/<%= artworks._id %>/edit">Edit artWork
                </a>
            </div>
            <p class="control">
                <form action="/artworkw/<%= artworks._id %>?_method=DELETE" method="POST">
                    <input class="button is-danger" type="submit" value="Delete artWork"/>                    
                </form>
            </p>
            </div>
        </div>
        </div>
    </article>
    </div>
</body>
</html> 