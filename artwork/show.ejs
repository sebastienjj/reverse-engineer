    <!DOCTYPE html>
    <html lang="en">
    <%- include ('../partials/head.ejs', {page: artWork.Title }) %>
      
    This is my nav bar now
    <%- include ('../partials/nav.ejs') %>

        <section class="hero is-medium has-background-light">
        <div class="hero-body">
            <h1 class="title">artWork Show Page</h1>
        </div>
        </section>

        <div class="container is-max-widescreen p-5">
        <article class="media">
            <figure class="media-left">
            <p class="image is-328x328">
                <img src="<%= artWork.Image %>" alt="artWork img" />
            </p>
            </figure>
            <div class="media-content">
            <div class="content">
                <h3>Title: <%= artWork.Title %></h3>
                <small>Artist: $<%= artWork.Artist %></small>
                <small>Mediums: $<%= artWork.Mediums %></small>
                <small>Date: $<%= artWork.Date %></small>
                <div class="field mt-5 is-grouped">
                <div class="control">
                    <a class="button is-info" href="/artWorks/<%= artWork._id %>/edit">Edit artWork
                    </a>
                </div>
                <p class="control">
                    <form action="/artWorks/<%= artWork._id %>?_method=DELETE" method="POST">
                        <input class="button is-danger" type="submit" value="Delete artWork"/>                    
                    </form>
                </p>
                </div>
            </div>
            </div>
        </article>
        </div>
    </body>
    </html> 